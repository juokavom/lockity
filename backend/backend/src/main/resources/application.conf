ktor {
    deployment {
        port = 5000
    }
    application {
        modules = [ lockity.ApplicationKt.main ]
    }
}
database {
    url = "jdbc:mysql://localhost:3306/lockity"
    user = "root"
    password = "root"
}